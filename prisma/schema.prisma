generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model Setting {
  id       Int    @id @default(autoincrement())
  ownerKey String @default("ron")
  key      String
  value    String

  @@unique([ownerKey, key])
}

model ItemWatch {
  id           Int     @id @default(autoincrement())
  ownerKey     String  @default("ron")
  itemName     String
  category     String  @default("consumable")
  minTarget    Int     @default(0)
  alertEnabled Boolean @default(true)

  @@unique([ownerKey, itemName])
}

model Snapshot {
  id        Int      @id @default(autoincrement())
  ownerKey  String   @default("ron")
  type      String
  data      String
  createdAt DateTime @default(now())
}
